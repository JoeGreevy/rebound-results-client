
type Cohorts = {
    labels: string[];
    values: string[];
};

export const cohorts = {
        labels : ["Footballers", "Sprinters", "Recreational"],
        values : ["footballer", "sprinter", "recreational"]
    }